# Pinned to the versions this backend is known to run on (validated locally:
# configure_mappers() OK, passlib+bcrypt hash/verify round-trip OK).
# Direct dependencies only — transitives resolve from these.

fastapi==0.133.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
asyncpg==0.31.0
pydantic==2.13.4
pydantic-settings==2.1.0
python-dotenv==1.0.0

# passlib 1.7.4 reads bcrypt's version at import time and the two are fragile
# together, so bcrypt is pinned explicitly rather than left to resolve — staff
# login depends on this pair.
passlib[bcrypt]==1.7.4
bcrypt==4.3.0

# JWT is python-jose (`from jose import jwt`). PyJWT was listed here but is not
# installed locally and nothing imports it — dropped rather than pinned to an
# untested version.
python-jose==3.3.0

python-multipart==0.0.6
reportlab==4.4.10
qrcode==8.2
httpx>=0.27

# FCM HTTP v1 sending (migration 014). Mints the OAuth access token from the
# Firebase SERVICE ACCOUNT — see PushService._access_token, which imports this
# lazily so the app stays importable if the install is ever missing.
#
# Nothing else in this file pulls google-auth in transitively, so without this
# line every push fails with "no access token" even when PUSH_ENABLED=true and
# the service-account file is mounted. Pinned to the version validated locally.
google-auth==2.53.0

# --- test-only -----------------------------------------------------------
# The API suite (tests/test_api_*.py) runs in-process against the ASGI app via
# httpx.ASGITransport, so no server and no network is involved. httpx is
# already a runtime dependency above.
#
# asyncio_default_fixture_loop_scope=session in pytest.ini is load-bearing:
# the app's SQLAlchemy engine is a module-level singleton, and a per-test event
# loop leaves its pooled connections bound to a closed loop — which shows up as
# errors that only appear when tests run together, never in isolation.
pytest==9.1.1
pytest-asyncio==1.4.0
